# Demo loop — memory

The loop reads this first and updates it last. It records each run so a re-run
starts knowing what already happened (and so you can see memory working).

## Runs

| date | result | attempts | notes |
|---|---|---|---|
| (none yet) | — | — | seed — run `/demo-loop` or `$demo-loop` to populate |

## Known-fixed (skip re-investigating)

| symbol | bug | fix |
|---|---|---|
| (none yet) | | |
